apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kanban-helm.fullname" . }}-test-connection"
  labels:
    {{- include "kanban-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kanban-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
